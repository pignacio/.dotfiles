============
Installation
============

At the command line::

    $ easy_install dotfiles_scripts

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dotfiles_scripts
    $ pip install dotfiles_scripts
